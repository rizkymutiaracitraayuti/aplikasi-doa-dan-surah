<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.projectkuliah">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ikon_aplikasi"
        android:label="Doa - Doa"
        android:roundIcon="@mipmap/ikon_aplikasi"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activities.DoaSehariActivity"
            android:exported="true"/>

        <activity
            android:name=".activities.SurahPendekActivity"
            android:exported="true"/>

        <activity
            android:name=".activities.MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>